{
  "name": "mqtt-proxy",
  "version": "1.0.0",
  "description": "MQTT to WebSocket proxy",
  "main": "mqtt-proxy.js",
  "dependencies": {
    "aedes": "^0.50.0",
    "mqtt": "^5.3.5",
    "websocket-stream": "^5.5.2"
  }
}
